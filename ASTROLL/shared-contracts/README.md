# shared-contracts

Общие контракты ASTROLL: DTO, события и идентификаторы, которыми обмениваются сервисы.

Контракты не содержат бизнес-логики сервисов. Они фиксируют публичный язык взаимодействия между `session`, `board`, `characters`, `dice` и `realtime_worker`.

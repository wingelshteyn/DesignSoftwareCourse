Министерство науки и высшего образования РФ
Федеральное государственное автономное
образовательное учреждение высшего образования
«СИБИРСКИЙ ФЕДЕРАЛЬНЫЙ УНИВЕРСИТЕТ»
Институт космических и информационных технологий
Программная инженерия

ОТЧЕТ О ПРАКТИЧЕСКОЙ РАБОТЕ №8
**Реструктуризация архитектуры**
тема: **ASTROLL — веб-платформа для проведения настольных ролевых игр онлайн**

Преподаватель: Д. В. Грузенкин
Студент: КИ23-17/1Б, 032320072, Белоглазов Владислав Владимирович

Красноярск 2026

---

## СОДЕРЖАНИЕ

1 Цель
2 Задачи
3 Ход выполнения
3.1 Выбор и описание системы для проектирования
3.2 Составление списка бизнес-требований
3.3 Выявление требований пользователя
3.4 Составление списка функциональных требований
3.5 Составление списка нефункциональных требований
3.6 Выбор архитектурного паттерна
3.7 Реализация карты контекстов по методологии DDD
3.8 Диаграмма контекста нотации C4
3.9 Реализация паттернов проектирования
3.10 Реализация принципов SOLID
3.11 Реализация принципов организации компонентов
3.12 Реализация аспектов безопасности
3.13 Реструктуризация архитектуры
4 Выводы

---

1 Цель

Выбрать тему для системы, архитектура которой будет разрабатываться в течение семестра, дать ей название и описать назначение.

Провести сбор и анализ требований к системе, архитектура которой будет разрабатываться в этом семестре.

Выбрать архитектурный паттерн для проектируемой системы и обосновать выбор.

Описать систему с помощью методологии DDD и C4.

Реализовать паттерны проектирования данной системы.

Реализовать принципы SOLID и принципы организации компонентов.

Обеспечить повышение безопасности проекта.

Провести реструктуризацию архитектуры проекта на основе атрибутов качества, выделенных для проекта с новыми требованиями.

2 Задачи

Для выполнения практической работы необходимо выполнить следующие задачи:

- придумать систему, её название и назначение;
- построить UML-диаграмму вариантов использования;
- предоставить отчёт с выполненными заданиями.

Составить список требований для проектируемой системы на следующих уровнях:

- бизнес-требования (для их составления вам понадобится пусть и вымышленный, но конкретный заказчик);
- требования пользователей;
- функциональные требования;
- нефункциональные требования.

Описать источники и методы получения требований, которые бы вы использовали в реальной работе с заказчиком по проектируемой системе.

Выбрать один из архитектурных паттернов и реализовать в коде. Бизнес-логику приложения писать не нужно: реализуем только связи между классами и модулями; для сервисной архитектуры достаточно структуры проектов и связей между ними.

В отчёте обосновать выбор реализованного шаблона в виде ADR (Architecture Decision Record).

Реализовать карту контекстов по методологии DDD. Для смыслового ядра и одной вспомогательной подобласти привести диаграмму контекста нотации C4 и описать каждый рисунок.

Реализовать в коде три паттерна проектирования (порождающий, структурный, поведенческий), построить UML-диаграмму классов и обосновать выбор; для каждого вида привести пример нецелесообразного паттерна.

Реализовать три принципа SOLID и четыре принципа организации компонентов, отразить код на диаграммах C4.

Обеспечить повышение безопасности проекта не менее чем тремя способами, описать компромиссы и привести обновлённые диаграммы компонентов и контейнеров C4.

Выбрать 2–3 атрибута качества, реструктуризовать архитектуру (переход `ASTROLL_OLD` → `ASTROLL`), описать причины перехода, привести диаграммы контекста и контейнеров C4 для старой и новой архитектуры, описать обновлённую архитектуру в нотации 4+1.

3 Ход выполнения

3.1 Выбор и описание системы для проектирования

В качестве системы для проектирования был выбран проект **ASTROLL** — реализация которого представлена в репозитории двумя версиями: модульный монолит `ASTROLL_PrPO/ASTROLL_OLD` и сервисная архитектура `ASTROLL_PrPO/ASTROLL`. Изначально система проектировалась и реализовывалась как монолит; после роста требований к масштабируемости и отказоустойчивости была выполнена реструктуризация (см. п. 3.13).

ASTROLL — веб‑ориентированная система для проведения настольных ролевых игр онлайн
(VTT, Virtual Tabletop). Система предназначена для хранения и редактирования
персонажей и заметок пользователей и организации игровой сессии в формате
“комнаты”, где участники видят общую доску, совершают игровые действия и
синхронно взаимодействуют в реальном времени.
Назначение системы Astroll заключается в предоставлении мастеру и
игрокам удобного пространства для ведения игровой сессии: общей доски,
добавления и редактирования изображений, создание рисунков на доске,
добавление и редактирование изображений, управления
персонажами(совершение бросков, перемещение по доске) и отображения их
токенов(иконок), выполнения бросков кубов, а также хранения
пользовательских данных (профиль, друзья, персонажи, заметки) и
сохранения/загрузки досок, создание и редактирование персонажей.
В системе предусмотрены две основные роли: Мастер и Игрок.
Мастер (создатель комнаты) организует сессию, ведет заметки,
сохраняет и загружает доски, а также управляет участниками комнаты: может
ограничивать права (режим “только просмотр”) и исключать игроков из
комнаты.
Игрок подключается к комнате по ссылке, просматривает и при
наличии прав редактирует доску, использует лист персонажа и участвует в
игровых действиях (в том числе бросках кубов), результаты которых
отображаются в комнате.
Система включает следующие основные функции:
- регистрация и авторизация пользователя;
- управление интерактивной доской (добавление пользователей,
управление правами пользователя, добавление изображений и текста, рисовать
на доске, добвалять и редактировать фигуры, сделать бросок кубов за
выбранного персонажа, добавить токен-иконку персонажа) ;
- управление профилем (никнейм, аватар), поиск пользователей,
добавление/удаление друзей;
- создание, просмотр, редактирование и удаление персонажей;
- подключение к игровой комнате и совместная работа на доске;
- синхронизация состояния доски и событий в реальном времени;
- броски кубов, зависящие от характеристик персонажа и просмотр
истории бросков;
- ведение заметок мастера (журнал страниц) и сохранение их вместе
со “снапшотом” доски;
- сохранение/загрузка досок (snapshots) из базы данных.
UML-диаграмма вариантов использования представлена на рисунках 1 и 2.
Рисунок 1 – UML-диаграмма вариантов использования системы (только для
роли «Игрок»)
Рисунок 2 – UML-диаграмма вариантов использования системы (только для
ролей «Мастер»)
3.2 Составление списка бизнес-требований
В качестве заказчика выступает вымышленная организация — клуб
настольных ролевых игр «Энросс‑Клуб», которая проводит регулярные сессии и
хочет иметь единый веб‑инструмент для:
- стандартизированного ведения листов персонажей;
- совместной онлайн‑доски и реалтайм‑взаимодействия;
- уменьшения ручной рутины мастера и игроков.
Для получения требований в реальной разработке целесообразны:
- интервью со стейкхолдерами (организатор, мастера, игроки);
- наблюдение за проведением сессии (офлайн/онлайн);
- анализ артефактов: шаблоны листов, правила, привычные инструменты
(Discord, Google Docs);
- воркшоп по приоритизации (MoSCoW), уточнение MVP.
Проект: «веб‑система для проведения настольных ролевых игр»
Дата проведения: 21.02.2026
Интервьюер: Белоглазов Владислав Владимирович
Интервьюируемые:
- Стейкхолдер 1 – Кузнецов Артём Сергеевич, организатор клуба ведущий
игр
- Стейкхолдер 2 – Кузнецова Алина Игоревна, соорганизатор клуба,
активный игрок и ведущий игр
Проблематика: Отсутсвие централизованного инструмента для проведения
игр и хранения листов персонажей, cложность ведения онлайн‑сессий
(карты/схемы/броски)
Вопрос № 1:
Тайминг вопроса: 00:02-00:07
Текст вопроса: Как сейчас ведутся листы персонажей?
Таймкод: 00:03
Ответы на вопрос:
Стейкхолдер 1 – Сейчас хранение осуществляется в файлах/таблицах
(Google Docs/Sheets) или в физическом формате на бумаге, у каждого игрока
отдельная копия, мастеру сложно контролировать и отслеживать персонажей
игроков, потому что у всех игроков лист персонажа в разных форматах.
Стейкхолдер 2 – Меня не устраивает хранить данные персонажа в Google
Docs, я хочу чтобы лист персонажа работал на погружение во время игры, а сухая
таблица с цифрами мне нравится меньше, чем бумажный вариант с рукописными
записями.
Вопрос № 2:
Тайминг вопроса: 00:07-00:13
Текст вопроса: Что наиболее мешает во время игровой сессии?
Таймкод: 00:08
Ответы на вопрос:
Стейкхолдер 1 – Во-первых, это время. Процесс определения
характеристик для бросоков и конроль игроков занимает огромное количество
времени. Во-вторых, человеческий фактор – кто-то непременно перепутает
куда смотреть для броска или ошибется в расчетах. Это приходится
перепроверять и отвлекаться от повествования.
Стейкхолдер 2 – Пересылка картинок для описания мастером, это занимет
слишком много времени и приходится отвлекаться на чат мессенджера.
Вопрос № 3
Тайминг вопроса: 00:13-00:18
Текст вопроса: Какие функции критичны для MVP?
Таймкод: 00:14
Ответы на вопрос:
Стейкхолдер 1 – Удобная в использовании интерактивная доска с
отслеживанием бросков игроков.
Стейкхолдер 2 – Удобный лист персонажа с красивым визуалом и
автосохранением.
Вопрос № 4
Тайминг вопроса: 00:18-00:24
Текст вопроса: Как измерить успех внедрения?
Таймкод: 00:19
Ответы на вопрос:
Стейкхолдер 1 – Сокращение времени на подготовку к игре и
механические процессы(на 25-30%), такие как броски кубов или редактирование
листа персонажа. Также сократить время синхронизации игрового поля у
игроков. Необходимо чтобы доска работала синхронно у всех игроков, с
задежкой в пределах нормы(2-3 с).
По результатам проведенного интервью был сформулирован список
бизнес-требований, каждый пункт списка содержит цели предприятия и
ожидаемый эффект от возможного решения.
Бизнес-требования заказчика:
1. Стандартизировать ведение листов— система должна предоставить
единый формат листа персонажа, чтобы информация была полной и
согласованной.
2. Сократить время на подготовку и ведение сессии — уменьшить ручную
рутину и поиск данных по персонажам.
3. Обеспечить онлайн‑взаимодействие— предоставить доску (VTT) и
синхронизацию действий в реальном времени.
4. Обеспечить сохранность данных — листы и доски должны храниться в
базе и быть доступны после перезапуска/перезахода.
5. Сделать продукт доступным и простым — работа в браузере, понятная
навигация, минимум шагов для старта.
3.3 Выявление требований пользователя
Для выявления требований пользователей была проведена вымышленная
беседа с посетителями клуба настольных игор, среди которых как мастеры так и
игроки.
Интервьюер: Белоглазов Владислав Владимирович
- Пользователь 1:
Вопрос № 1: Представьтесь, пожалуйста. Кто вы и чем занимаетесь?
Ответ на вопрос: Я игрок. Обычно веду одного‑двух персонажей в разных
кампаниях, между сессиями обновляю лист, на игре быстро смотрю значения
характеристик и заметки. Я хочу иметь возможность редактировать свой
профиль, а также искать и добавлять других пользователей в друзья.
Вопрос № 2: Что вам было бы удобно иметь в новой системе?
Ответ на вопрос: Хочу, чтобы лист персонажа был в одном месте и всегда
актуальный. Чтобы можно было быстро править значения прямо во время игры
и не думать, что “у меня старая копия”. Ещё важно уметь сохранить лист в файл
и перенести/отправить мастеру при необходимости.
Вопрос № 3: Нужна ли вам совместная доска и участие в комнате мастера?
Ответ на вопрос: В онлайне хочется, чтобы у всех была одна и та же доска,
без пересылки скриншотов. Мне удобно просто зайти в комнату и видеть
изменения в реальном времени.
- Пользователь 2:
Вопрос № 1: Представьтесь, пожалуйста. Кто вы и чем занимаетесь?
Ответ на вопрос: Я мастер. Веду сессии, готовлю материалы, управляю
сценами/боем, слежу за тем, чтобы у всех была одинаковая информация и чтобы
игра не тормозилась из‑за технических мелочей.
Вопрос № 2: Что для вас важно в новой системе?
Ответ на вопрос: Мне нужна доска, на которой я могу вести сцену и
которую видят игроки. Важно, чтобы доска не “разъезжалась” у разных людей,
чтобы можно было быстро продолжить с того же состояния. Также хочу видеть,
кто подключён к комнате, и иметь возможность отключить интерактивность,
если кто‑то мешает (только просмотр), или кикнуть..
- Пользователь 3:
Вопрос № 1: Представьтесь, пожалуйста. Кто вы и чем занимаетесь в
клубе?
Ответ на вопрос: Я игрок. Я редко играю в настольные игры и обычно мне
требуется большое количество времени чтобы разобраться в правилах и листе
персонажа.
Вопрос № 2: Что для вас важно в новой системе?
Ответ на вопрос: Мне было бы удобно с понятным листом персонажа и
кнопкой броска кубов, которая сама считает сколько кубов нужно кинуть и какой
результат.
Полученное интервью-опрос можно использовать для документированя
требований пользователей. Для их представления было принято решение
использовать пользовательские истории (User Story) для двух ролей:
1) Мастер – главный пользователь системы, чья задача проводить игры в
интерактивной доске, собирать вместе игроков, помогать им генерировать
персонажей.
- КАК мастер, Я ХОЧУ удобный редактируемый лист персонажа, ЧТОБЫ
было удобнее создавать персонажей для игроков;
- КАК мастер, Я ХОЧУ иметь интерактивную доску с
масштабированием/перемещением, ЧТОБЫ вести сцены и бои;
- КАК мастер, Я ХОЧУ правлять доступом к доске (readonly) и исключать
нарушителей (kick), ЧТОБЫ контролировать сессию;
- КАК мастер, Я ХОЧУ видеть историю бросков, ЧТОБЫ разбирать
спорные моменты.
2) Игрок – человек, который будет хранить своих персонажей и
участвовать в играх, которые проводит мастер;
- КАК игрок, Я ХОЧУ зарегистрироваться и войти в систему, ЧТОБЫ
всегда иметь доступ к своим данным и персонажам;
- КАК игрок, Я ХОЧУ создавать несколько персонажей, ЧТОБЫ
участвовать в разных кампаниях;
- КАК игрок, Я ХОЧУ редактировать лист персонажа, ЧТОБЫ быстро
обновлять информацию во время игры;
- КАК игрок, Я ХОЧУ подключаться к комнате и видеть доску в реальном
времени, ЧТОБЫ играть онлайн.
3.4 Составление списка функциональных требований
Функциональные требования были получены декомпозицией бизнестребований и требований пользователей. Список функциональных требований:
FRQ‑1. Аутентификация и авторизация
- FRQ‑1.1 Система должна предоставлять пользователю возможность
регистрации по login и паролю;
- FRQ‑1.2 При регистрации система должна валидировать login и
минимальную длину пароля (6 символов);
- FRQ‑1.3 Система должна предотвращать повторную регистрацию на уже
существующий login и возвращать понятную ошибку;
- FRQ‑1.4 Система должна предоставлять пользователю возможность
входа по login и паролю;
- FRQ‑1.5 При входе система должна возвращать токен авторизации (JWT)
и данные пользователя;
- FRQ‑1.6 Система должна использовать JWT‑токен в заголовке
`Authorization: Bearer <token>` для авторизации запросов к защищённым
ресурсам;
- FRQ‑1.7 Система должна ограничивать доступ к основным разделам UI
без авторизации (перенаправление на страницу входа);
- FRQ‑1.8 Система должна позволять пользователю завершить сессию
(logout) путём удаления локального токена на клиенте.
FRQ‑2. Профиль пользователя и друзья
- FRQ‑2.1 Система должна предоставлять просмотр собственного профиля;
- FRQ‑2.2 Система должна позволять изменять nickname и avatar;
- FRQ‑2.3 Система должна позволять изменять login и пароль;
- FRQ‑2.4 При смене пароля система должна сохранять только хеш (без
хранения исходного пароля);
- FRQ‑2.5 Система должна предоставлять поиск пользователей по строке
запроса (по login/нику);
- FRQ‑2.6 Система должна позволять добавлять пользователя в друзья и
удалять из друзей;
- FRQ‑2.7 Система должна запрещать добавлять в друзья самого себя;
- FRQ‑2.8 Система должна позволять просматривать профиль другого
пользователя по id (в рамках авторизованного доступа).
FRQ‑3. Персонажи
- FRQ‑3.1 Система должна предоставлять список персонажей текущего
пользователя;
- FRQ‑3.2 Система должна возвращать список персонажей в
отсортированном виде (например, по времени последнего изменения);
- FRQ‑3.3 Система должна позволять создать нового персонажа;
- FRQ‑3.4 При создании персонажа система должна инициализировать
базовые данные листа (дефолтный `sheetData`);
- FRQ‑3.5 Система должна позволять получить персонажа по id;
- FRQ‑3.6 Система должна запрещать доступ к персонажу по id, если он
принадлежит другому пользователю;
- FRQ‑3.7 Система должна позволять обновлять персонажа частично
(PATCH): имя, `sheetData`, `schemaVersion`;
- FRQ‑3.8 Система должна удалять персонажа и связанные данные по
запросу владельца (DELETE).
FRQ‑4. Лист персонажа
- FRQ‑4.1 Система должна хранить лист персонажа в JSON‑формате
`sheetData`;
- FRQ‑4.2 Формат `sheetData` должен включать метаданные (`meta`) с
`schemaVersion` и датой создания;
- FRQ‑4.3 Система должна поддерживать многостраничность листа
(минимум page1 + дополнительные страницы);
- FRQ‑4.4 Система должна хранить портреты персонажа (dataUrl) и
поддерживать кадрирование/выбор основного портрета;
- FRQ‑4.5 Система должна поддерживать авто‑сохранение (debounce) и
отправку изменений в API;
- FRQ‑4.6 Система должна показывать пользователю статус сохранения
(например, “Сохранение…”);
- FRQ‑4.7 Система должна поддерживать импорт листа из JSON файла (с
проверкой структуры данных);
- FRQ‑4.8 Система должна поддерживать экспорт листа в JSON файл;
- FRQ‑4.9 Система должна обеспечивать совместимость данных при
изменении структуры (merge, schemaVersion).
FRQ‑5. Режим мастера (комнаты, доска, реалтайм)
- FRQ‑5.1 Система должна позволять подключаться к комнате по `roomId`;
- FRQ‑5.2 Система должна поддерживать присоединение к комнате как с
токеном (авторизованный пользователь);
- FRQ‑5.3 Система должна синхронизировать состояние доски между
участниками комнаты в реальном времени (Socket.IO);
- FRQ‑5.4 Система должна предоставлять снапшот доски для вновь
подключившихся участников (board:get → board:snapshot);
- FRQ‑5.5 Система должна хранить (кешировать) последнее состояние
доски в рамках комнаты на сервере на время работы процесса;
- FRQ‑5.6 Система должна предоставлять список присутствующих игроков
(presence/roster) в комнате и обновлять его при входе/выходе;
- FRQ‑5.7 Система должна определять “мастера/хоста” комнаты и давать
ему права управления;
- FRQ‑5.8 Система должна позволять мастреу комнаты переключать
игрокам readonly и выполнять kick;
- FRQ‑5.9 Система должна уведомлять участников о смене прав (readonly)
и факте исключения (kick).
FRQ‑6. Броски и история
- FRQ‑6.1 Система должна поддерживать передачу результатов бросков из
листа персонажа в комнату (доску);
- FRQ‑6.2 Сообщение о броске должно содержать минимум: автора (или
“аноним”), тип/описание броска и итоговое значение;
- FRQ‑6.3 Система должна отображать историю бросков в UI;
- FRQ‑6.4 История бросков должна обновляться в реальном времени без
перезагрузки страницы.
FRQ‑7. Хранение досок в БД
- FRQ‑7.1 Система должна сохранять доски пользователя как сущность
`Board` с JSON‑снапшотом `snapshot`;
- FRQ‑7.2 Система должна предоставлять CRUD‑операции для досок
(создать/список/получить/обновить/удалить);
- FRQ‑7.3 Система должна запрещать доступ к доскам другого
пользователя;
- FRQ‑7.4 Система должна позволять переименовывать доску без потери
снапшота.
FRQ‑8. Диагностика
- FRQ‑8.1 Система должна предоставлять эндпоинт `GET /api/health` для
проверки доступности API и БД;
- FRQ‑8.2 Эндпоинт должен возвращать признак доступности базы и
понятное сообщение при проблемах;
- FRQ‑8.3 Ошибки API должны возвращаться в JSON‑формате с
человекочитаемым описанием.
3.5 Составление списка нефункциональных требований
NFRQ-1. Безопасность:
- NFRQ‑1.1 Система должна хранить пароли в виде безопасного хеша
(bcrypt);
- NFRQ‑1.2 Система не должна хранить пароли в открытом виде и не
должна логировать их;
- NFRQ‑1.3 Система должна предотвращать доступ к чужим персонажам и
доскам (проверка владельца на сервере);
- NFRQ‑1.4 Система должна обеспечивать защищённую передачу данных
в продакшене (HTTPS/TLS на уровне хостинга/прокси);
- NFRQ‑1.5 Система должна валидировать входные данные на бэкенде
(например, Zod) и возвращать 4xx при ошибках валидации;
- NFRQ‑1.6 Система должна использовать секрет подписи JWT,
настраиваемый через переменные окружения.
NFRQ‑2. Удобство использования
- NFRQ‑2.1 Интерфейс должен быть веб‑ориентированным и доступным в
актуальных версиях браузеров: yandex, firefox, opera, google chrom;
- NFRQ‑2.2 Основные сценарии (регистрация → создание персонажа →
редактирование → режим мастера) должны выполняться без длительного
обучения;
- NFRQ‑2.3 Авто‑сохранение не должно приводить к потере
пользовательских данных и не должно мешать вводу;
- NFRQ‑2.4 UI должен выдавать понятные сообщения об ошибках
(например, “нет соединения с сервером”, “нужна авторизация”).
NFRQ‑3. Производительность и реалтайм
- NFRQ‑3.1 Система должна быть отзывчивой при типовой нагрузке малой
группы (2–8 игроков);
- NFRQ‑3.2 Реалтайм‑синхронизация доски должна работать без
заметных задержек для участников(2-3 с);
- NFRQ‑3.3 Операции сохранения листа (PATCH) не должны блокировать
интерфейс пользователя.
NFRQ‑4. Надёжность
- NFRQ‑4.1 Данные персонажей и досок должны сохраняться в БД и быть
доступны после перезапуска сервера;
- NFRQ‑4.2 Система должна корректно обрабатывать временную
недоступность БД и возвращать корректные коды ошибок (например, 503);
- NFRQ‑4.3 При проблемах с БД система должна возвращать понятные
сообщения, достаточные для диагностики (что проверить в окружении);
- NFRQ‑4.4 Система должна сохранять целостность данных при частичных
обновлениях (PATCH).
3.6 Выбор архитектурного паттерна
Дальше необходимо было выбрать архитектурный паттерн для данной
системы. Выбранный паттерн и обоснование выбора были описаны в виде ADR
(Architecture Decision Record):
Запись принятого решения №1: Выбор архитектурного паттерна для
системы
Дата: 2026-03-05
Контекст:
Выделены главные функциональные ограничения: низкая задержка и
масштабирование по комнатам (NFRQ‑2.1, NFRQ‑2.2); отказоустойчивость
подсистем (NFRQ‑3.1); наблюдаемость и управляемые релизы (NFRQ‑4.2);
развитие доменов с явными границами в коде и возможность горизонтального
масштабирования без обязательного дробления на множество сервисов.
Решение:
Модульный монолит: один исполняемый backend (`ASTROLL_OLD/backend`) и единый цикл выпуска,
внутри — доменные модули и общая реляционная база данных; бинарные медиа
выносятся во внешнее объектное хранилище.
Целевой набор модулей (каждый отвечает за свой контекст, данные — в
общей БД):
1. `accounts` — регистрация/вход, JWT, профиль, `RBACPolicy`.
2. `friends` — заявки в друзья, список друзей.
3. `characters` — персонажи, листы, `CharacterAccessPolicy`.
4. `rooms` — комнаты, членство, права в комнате.
5. `board` — элементы доски, адаптер Excalidraw, снапшоты.
6. `dice` — броски и история бросков в комнате.
7. `game_session` — смысловое ядро: команды, политика сессии, публикация событий.
8. `administration` — эксплуатационные настройки, `AdministrationPolicy`.

Реализация размещена в каталоге `ASTROLL_PrPO/ASTROLL_OLD`.
Последствия:
Преимущества: один деплой и одна БД — проще эксплуатация, бэкапы и
миграции; проще согласованность данных в сценариях комнаты; меньше
сетевых контрактов для небольшой команды; модули дают обзорность; при
росте нагрузки — горизонтальное масштабирование.
Риски: масштабируется весь процесс целиком; сбой или дефект в модуле
теоретически влияют на процесс (смягчается слоями, тестами, мониторингом);
нужна дисциплина границ модулей.
Для ASTROLL такой баланс предпочтительнее полноценной
микросервисной платформы на этапе MVP. После роста нагрузки выполнен переход к сервисной архитектуре `ASTROLL` (см. п. 3.13).
Альтернативы:
В качестве альтернативы рассматривалась
микросервисная архитектура. Такой подход делает систему менее связанной и
даёт возможности масштабирования, а также делает систему более
отказоустойчивой, но значительно усложняет инфраструктуру и разработку
модулей.
Также в качестве альтернативы модульному монолиту веб-приложения
рассматривалась слоистая архитектура. Она проще в реализации, но логика
работы выделенных главных модулей становится размазанной по всем слоям,
что усложняет поддержку кода и реализацию изменений.
3.7 Реализация карты контекстов по методологии DDD
Далее проект «Astroll» был проанализирован с точки зрения методологии
DDD.
Главный домен (бизнес-домен) данной системы — проведение
онлайн‑настольной ролевой сессии: совместная интерактивная доска (VTT),
синхронизация действий участников в реальном времени, управление комнатой
и игровыми событиями (в том числе бросками кубов).
Для него были выделены поддомены:
 а) Смысловое ядро:
 1) Игровая сессия (VTT). Это ключевая бизнес‑ценность системы:
мастер и игроки работают в одной «комнате» на общей доске, видят присутствие
участников, права доступа и результаты игровых действий без
рассинхронизации состояния.
 б) Вспомогательные поддомены:
1) Персонажи и листы — хранение, редактирование и
импорт/экспорт листов персонажа (включая данные стэнда).
2) Хранение досок — сохранение и загрузка снапшотов досок
пользователя в базе данных.
 3) Комнаты и присутствие — идентификатор комнаты, roster
участников, назначение хоста (мастера), приглашения.
4) Броски кубов — формирование и трансляция записей о бросках в
комнату, история бросков.
5) Друзья — социальные связи между пользователями
(добавление/удаление, поиск).
 в) Поддомены общего назначения:
 1) Учётные записи и роли — регистрация, вход, JWT‑авторизация,
профиль пользователя. Для аутентификации можно опираться на типовые
решения (хеширование паролей, выдача токенов).
Исходя из данных поддоменов и в соответствии с выбранной архитектурой
(модульный монолит, см. ADR в работе №3) были определены ограниченные
контексты и связи между ними, из которых составлена карта контекстов,
показанная на рисунке 3.
Рисунок 3 – Карта контекста системы по методологии DDD
Ограниченные контексты, приведённые на карте контекстов:
1) Игровая сессия (VTT) — центральный контекст смыслового ядра. Он
координирует реалтайм‑синхронизацию доски (Socket.IO), применяет права
мастера (readonly, kick), использует данные персонажей и снапшоты досок. Для
остальных контекстов, которым нужны игровые события и состояние сессии, он
выступает Upstream.
2) Accounts (учётные записи и роли) — контекст регистрации, входа,
профиля и проверки доступа. Он задаёт единую модель пользователя (userId,
JWT), которую потребляют Friends, Characters и Game Session, поэтому связи с
ними оформлены как Conformist (Downstream подстраивается под модель
Accounts).
3) Characters (персонажи и листы) — контекст CRUD персонажей,
многостраничного листа, портретов, авто‑сохранения и импорта/экспорта JSON.
Он поставляет sheetData и связанные данные в Game Session для бросков и
токенов на доске — связь Customer‑Supplier (Characters — поставщик, Game
Session — потребитель).
4) Board Storage (хранение досок) — контекст сохранения снапшотов
Excalidraw в сущности Board. Загруженные снапшоты передаются в активную
сессию — связь Customer‑Supplier с Game Session.
5) Rooms (комнаты и присутствие) — контекст roomId, roster, claimHost и
приглашений. Game Session опирается на него как на поставщика метаданных
комнаты — Customer‑Supplier (Rooms — поставщик).
6) Dice (броски кубов) — контекст roll:entry и истории бросков в комнате.
С Game Session он связан Partnership: оба тесно участвуют в игровом процессе и
развиваются согласованно; при этом формат событий изолируется внутри
домена.
7) Friends (друзья) — контекст заявок и списка друзей, поиска
пользователей. Подчиняется модели Accounts (Conformist).
8) Excalidraw (внешний) — внешняя библиотека холста для отрисовки и
редактирования доски. Для связи с Game Session используется Anti‑Corruption
Layer: ядро не должно зависеть от деталей API и внутреннего состояния виджета
Excalidraw.
3.8 Диаграмма контекста нотации C4
Для смыслового ядра и одного из вспомогательных поддоменов построены
диаграммы контекста нотации C4. Первая диаграмма показана на рисунке 4.
Рисунок 4 – Диаграмма контекста нотации C4 для смыслового ядра системы
Центральная система на диаграмме — смысловое ядро «Игровая сессия
(VTT)». Её основные пользователи — персоны Мастер и Игрок. Мастер ведёт
сессию, управляет комнатой, сохраняет доски и сценой; игрок подключается к
комнате, редактирует лист и при наличии прав — доску.
Ядро получает аутентификацию из поддомена общего назначения
«Учётные записи» (регистрация, JWT, профиль), данные комнаты и roster — из
«Комнаты», sheetData для бросков и токенов — из «Персонажи и листы»,
снапшоты — из «Хранение досок», события бросков обменивает с «Броски
кубов». Для отображения и редактирования поля используется внешняя система
Excalidraw через Canvas API и ACL.
На рисунке 5 показана диаграмма контекста нотации C4 для
вспомогательного поддомена «Персонажи и листы».
Рисунок 5 – Диаграмма контекста нотации C4 для вспомогательного поддомена
системы
Центральная система на диаграмме — вспомогательный поддомен
«Персонажи и листы»: CRUD персонажей, многостраничный лист, портреты и
стэнды, авто‑сохранение, импорт/экспорт JSON. Игрок создаёт и редактирует
персонажей между сессиями и правит лист во время игры.
Контекст опирается на «Учётные записи» (владелец персонажа, JWT,
проверка доступа по userId) и передаёт sheetData в смысловое ядро «Игровая
сессия» для бросков и токенов на доске. Подсказки по стэндам берутся из
внешнего артефакта «Справочник Энросс» (GuideForStadns.md) и отображаются
в UI.
3.9 Реализация паттернов проектирования

Далее в проектируемой системе ASTROLL были реализованы три паттерна проектирования, по одному для каждого вида. Их код написан на языке Python и размещён в смысловом ядре монолита `ASTROLL_OLD/backend/apps/`; аналогичные классы перенесены в сервисы `ASTROLL/services/board` и `ASTROLL/services/session`.

3.9.1 Порождающий паттерн

Для реализации порождающего паттерна в смысловом ядре системы выбран паттерн «Фабричный метод». Его UML-диаграмма классов показана на рисунке 6 (см. `diagram_prompts/06_factory_method.md`).

Рисунок 6 – Диаграмма классов реализации паттерна «Фабричный метод»

Паттерн применён в модуле `board` (`ASTROLL_OLD/backend/apps/board/elements.py`) для создания доменных объектов элементов доски: токен персонажа (`TokenElement`), изображение (`ImageElement`), текст (`TextElement`), фигура (`ShapeElement`), рисунок (`DrawingElement`). Абстрактный класс `BoardElementFactory` задаёт интерфейс создания объекта `BoardElement`; конкретные фабрики (`TokenElementFactory`, `ImageElementFactory` и др.) создают соответствующие типы элементов. Клиентом паттерна выступает `BoardUpdateBuilder`, который по типу входного намерения (`BoardActionPayload.element_type`) делегирует создание объекта соответствующей фабрике через реестр `BoardElementFactoryRegistry`.

Выбор паттерна обоснован тем, что система поддерживает несколько типов элементов доски, требует единообразного создания объектов и возможности расширения набора типов без изменения клиентского кода сценариев игровой сессии.

Порождающий паттерн «Абстрактная фабрика» в данном месте использовать нецелесообразно, поскольку не требуется одновременно подменять семейства взаимосвязанных объектов разных платформ или тем оформления; для поэлементного расширения доски достаточно фабричного метода.

3.9.2 Структурный паттерн

Для реализации структурного паттерна в смысловом ядре системы выбран паттерн «Адаптер». Его UML-диаграмма классов показана на рисунке 7 (см. `diagram_prompts/07_adapter.md`).

Рисунок 7 – Диаграмма классов реализации паттерна «Адаптер»

Паттерн применён в модуле `board` (`ASTROLL_OLD/backend/apps/board/adapters.py`) на границе с внешней библиотекой Excalidraw. Внешний клиент `ExcalidrawClient` возвращает сцену в формате `ExcalidrawJson`, не совпадающем с внутренней моделью `BoardSnapshot`. Класс `ExcalidrawSnapshotAdapter` реализует порт `BoardSnapshotProvider` и адаптирует интерфейс внешнего клиента к доменным структурам при загрузке и сохранении снапшотов. Адаптер выступает антикоррупционным слоем (ACL): ядро игровой сессии не зависит от деталей JSON Excalidraw.

Выбор паттерна обоснован необходимостью изолировать смысловое ядро от формата и технических деталей внешней библиотеки визуализации, а также обеспечить слабую связанность и удобство замены поставщика отрисовки доски.

Структурный паттерн «Компоновщик» в данном месте использовать нецелесообразно, поскольку элементы доски не образуют древовидную иерархию «часть-целое», требующую рекурсивной обработки.

3.9.3 Поведенческий паттерн

Для реализации поведенческого паттерна в смысловом ядре системы выбран паттерн «Команда». Его UML-диаграмма классов показана на рисунке 8 (см. `diagram_prompts/08_command.md`).

Рисунок 8 – Диаграмма классов реализации паттерна «Команда»

Паттерн применён в модуле `game_session` (`ASTROLL_OLD/backend/apps/game_session/commands.py`). Действия мастера и игроков на доске, броски кубов и управление правами в комнате представлены объектами-командами (`AddBoardElementCommand`, `MoveTokenCommand`, `RollDiceCommand`, `SetReadonlyCommand`, `KickPlayerCommand`, `SaveBoardSnapshotCommand`), реализующими общий интерфейс `GameSessionCommand`. Получателем команд выступает агрегат `GameSession`, а единым вызывающим объектом — `GameSessionCommandInvoker`, который проверяет права через `SessionPolicy`, записывает события в `SessionEventLog` и публикует их через `RealtimeEventPublisher`.

Выбор паттерна обоснован необходимостью единообразно представлять игровые и редакционные действия, централизованно выполнять их и фиксировать историю изменений при синхронизации сессии в реальном времени.

Поведенческий паттерн «Посредник» в данном месте использовать нецелесообразно, поскольку координация уже выражена агрегатом `GameSession`, политикой прав и инвокером команд.

3.10 Реализация принципов SOLID

Далее были реализованы три принципа SOLID в коде `ASTROLL_OLD` и перенесены в сервисную версию `ASTROLL`.

3.10.1 Принцип подстановки Барбары Лисков и открытости/закрытости

В реализации фабричного метода (п. 3.9.1) соблюдается принцип открытости/закрытости (OCP). Абстрактный класс `BoardElementFactory` задаёт общий контракт создания элемента, а абстрактный продукт `BoardElement` определяет общее представление результата через метод `to_snapshot()`. При появлении нового типа элемента достаточно добавить новый класс элемента и соответствующую фабрику, зарегистрировать её в `BoardElementFactoryRegistry` — код `BoardUpdateBuilder` менять не требуется.

Принцип подстановки Барбары Лисков (LSP) обеспечивается тем, что все конкретные элементы (`TokenElement`, `ImageElement`, `TextElement`, `ShapeElement`, `DrawingElement`) реализуют единый контракт `BoardElement`: клиентский код может работать с любым элементом через общий интерфейс `to_snapshot()` и `can_edit()` без проверки конкретного типа. Диаграмма C4 обновлённого кода с применением OCP и LSP показана на рисунке 9 (см. `diagram_prompts/09_solid_ocp_lsp.md`).

Рисунок 9 – Диаграмма кода C4 для принципа подстановки Барбары Лисков и принципа открытости/закрытости

3.10.2 Принцип инверсии зависимостей

Для реализации принципа инверсии зависимостей (DIP) инвертирована зависимость между `GameSessionCommandInvoker` и механизмами журналирования и публикации событий. Высокоуровневый инвокер команд не зависит от конкретных реализаций аудита и realtime-транспорта, а работает с абстракциями `SessionEventLog` и `RealtimeEventPublisher`. Конкретные классы `InMemorySessionEventLog` и `NullRealtimeEventPublisher` (в монолите) или их аналоги в `realtime_worker` (в сервисной версии) реализуют эти абстракции.

Диаграмма C4 обновлённого кода с применением DIP показана на рисунке 10 (см. `diagram_prompts/10_solid_dip.md`).

Рисунок 10 – Диаграмма кода C4 для принципа инверсии зависимостей

3.11 Реализация принципов организации компонентов

3.11.1 Принципы связности компонентов

Для определения основных компонентов использовался принцип REP (Reuse/Release Equivalence Principle): крупные бизнес-модули логично сопровождать и выпускать как единое целое. В `ASTROLL_OLD` компоненты соответствуют папкам `backend/apps/*`.

Принцип CCP (Common Closure Principle) применён на начальном этапе: классы группируются по общей причине изменения, чтобы одно функциональное изменение (например, правила бросков) не затрагивало несвязанные модули.

Компоненты монолита `ASTROLL_OLD`:

1. `accounts` — аутентификация, профиль, `RBACPolicy`;
2. `friends` — социальные связи;
3. `characters` — персонажи, листы, `CharacterAccessPolicy`;
4. `rooms` — комнаты, roster, права хоста;
5. `board` — элементы доски, адаптер Excalidraw;
6. `dice` — броски и история;
7. `game_session` — смысловое ядро, команды, политика сессии;
8. `administration` — эксплуатационные настройки, `AdministrationPolicy`.

3.11.2 Принципы сочетаемости компонентов

В качестве основных принципов сочетаемости использовались ADP (Acyclic Dependencies Principle) и SDP (Stable Dependencies Principle). Граф зависимостей зафиксирован в `ASTROLL_OLD/backend/apps/components.py` и проверяется функцией `has_cycles()`. На рисунке 11 показана диаграмма компонентов C4 (см. `diagram_prompts/11_c4_components.md`).

Рисунок 11 – Диаграмма компонентов C4 системы ASTROLL_OLD

Классы, реализующие зависимости компонентов:

- `RBACPolicy` в `accounts` задаёт стабильные правила доступа и ни от кого не зависит;
- `AdministrationPolicy` в `administration` опирается на `RBACPolicy`;
- `CharacterAccessPolicy` в `characters` проверяет владельца персонажа;
- `SessionPolicy` в `game_session` проверяет права хоста и режим readonly;
- `GameSessionCommandInvoker` в `game_session` зависит от абстракций лога и публикации;
- `ExcalidrawSnapshotAdapter` в `board` изолирует ядро от внешнего формата.

По диаграмме и описанию зависимостей ADP соблюдается: циклов в графе нет. Для подтверждения SDP проверено, что более неустойчивые компоненты зависят от более устойчивых:

- `game_session` → `board`, `dice`, `rooms`, `characters` → `accounts`;
- `administration` → `accounts`;
- `friends` → `accounts`.

3.12 Реализация аспектов безопасности

Далее были реализованы три способа обеспечения безопасности проекта.

3.12.1 Реализация управления доступом

Первый способ — ролевая модель доступа (RBAC) и политики на уровне домена. В системе три роли: **Игрок**, **Мастер**, **Администратор**.

- Игрок — создаёт персонажей, подключается к комнате, редактирует доску при наличии прав, делает броски;
- Мастер — создаёт комнату, управляет участниками (readonly, kick), ведёт сессию;
- Администратор — управляет пользователями и эксплуатационными настройками.

`RBACPolicy` (`ASTROLL_OLD/backend/apps/accounts/models.py`) определяет базовые права; `SessionPolicy` ограничивает команды в комнате; `CharacterAccessPolicy` запрещает доступ к чужим персонажам. В сервисной версии аналог — `AuthorizationPolicy` в `ASTROLL/services/auth_accounts/models/user.py`.

Для усиления безопасности политики доступа вынесены отдельно от логики хранения аккаунтов. Компромиссы:

1. Дополнительная проверка прав на каждый запрос к защищённому ресурсу;
2. При недоступности модуля `accounts` часть функций, требующих проверки роли, становится недоступной.

Для смягчения можно кешировать роли в памяти на время сессии.

3.12.2 Реализация шифрования данных

Второй способ — безопасное хранение учётных данных. Пароли не хранятся в открытом виде: по NFRQ-1.1 применяется хеширование (bcrypt). JWT подписывается секретом из переменных окружения (NFRQ-1.6). В продакшене передача данных защищается TLS на уровне API Gateway или reverse-proxy.

Компромиссы:

1. Хеширование и проверка пароля добавляют вычислительные затраты при входе;
2. Шифрование снапшотов досок и портретов в object storage (MinIO) повышает безопасность, но снижает производительность — применяется выборочно к наиболее критичным данным.

3.12.3 Реализация изоляции компонентов

Третий способ — контейнеризация и сетевое разделение. В `ASTROLL/docker-compose.yml` каждый сервис (`auth-accounts`, `session`, `board`, `realtime-worker` и др.) развёрнут в отдельном Docker-контейнере с минимальными привилегиями. Внешний трафик проходит через **API Gateway** (nginx), который выполняет маршрутизацию и rate limiting (защита от DoS). Сервисы не имеют прямого доступа к чужим базам данных.

Компромиссы:

1. Более сложная настройка сетей и межсервисного взаимодействия;
2. API Gateway становится потенциальной точкой отказа (смягчается репликацией и health-check).

3.12.4 Новые диаграммы компонентов и контейнеров нотации C4

В ходе реализации трёх способов обеспечения безопасности обновлена диаграмма компонентов C4 — рисунок 12 (см. `diagram_prompts/12_c4_components_security.md`).

Рисунок 12 – Обновлённая диаграмма компонентов нотации C4

Также составлена диаграмма контейнеров — рисунок 13 (см. `diagram_prompts/13_c4_containers_secure.md`).

Рисунок 13 – Диаграмма контейнеров нотации C4 с учётом безопасности

3.13 Реструктуризация архитектуры

Далее была смоделирована ситуация, когда заказчик (клуб «Энросс‑Клуб») просит адаптировать систему к возросшим нагрузкам: число одновременных онлайн-сессий выросло, пользователи жалуются на задержки синхронизации доски и падения при пиковых нагрузках.

Выбраны два наиболее важных атрибута качества:

1. **Масштабируемость** — необходимо горизонтально масштабировать только нагруженные подсистемы (`game_session`, realtime, `board`), не дублируя весь монолит;
2. **Отказоустойчивость** — падение одного модуля не должно приводить к полной недоступности платформы (регистрация и редактирование персонажей должны работать при временной недоступности realtime).

Принято решение перейти с модульного монолита `ASTROLL_OLD` на архитектуру на основе сервисов `ASTROLL`. Модульный монолит разделён на восемь сервисов:

| Модуль ASTROLL_OLD | Сервис ASTROLL |
|--------------------|----------------|
| `accounts` | `auth_accounts` |
| `friends` | `social` |
| `characters` | `characters` |
| `rooms` + `game_session` | `session` |
| `board` | `board` |
| `dice` | `dice` |
| `administration` | `administration` |
| realtime в процессе | `realtime_worker` |

Общие DTO вынесены в `ASTROLL/shared-contracts/python/astroll_shared_contracts/contracts.py` (`UserRef`, `BoardSnapshotDTO`, `SessionEventDTO`, `DiceRollDTO` и др.).

Переход обоснован тем, что сервисная архитектура позволяет независимо развёртывать и масштабировать `session`, `board` и `realtime_worker`, а сбой realtime-контура не блокирует CRUD персонажей и аутентификацию. Подход сохраняет более крупные границы сервисов и не усложняет систему до уровня «чистых» микросервисов с отдельной БД на каждый сервис.

Диаграмма контекста нотации C4 не меняется — рисунок 14 (см. `diagram_prompts/14_c4_context_system.md`).

Рисунок 14 – Диаграмма контекста нотации C4 системы ASTROLL

Диаграмма контейнеров до реструктуризации (`ASTROLL_OLD`) — рисунок 15 (см. `diagram_prompts/15_c4_containers_before_restructuring.md`).

Рисунок 15 – Диаграмма контейнеров нотации C4 до реструктуризации (ASTROLL_OLD)

Диаграмма контейнеров после реструктуризации (`ASTROLL`) — рисунок 16 (см. `diagram_prompts/16_c4_containers_after_restructuring.md`).

Рисунок 16 – Диаграмма контейнеров нотации C4 после реструктуризации (ASTROLL)

Архитектура после реструктуризации описана в нотации 4+1.

**Логическое представление** (рисунок 17, `diagram_prompts/17_4plus1_logical_view.md`): система разделена на восемь сервисов, соответствующих ограниченным контекстам DDD, и инфраструктурные контейнеры (API Gateway, PostgreSQL, Redis, Object Storage). Сервисы обмениваются данными через `shared-contracts`.

**Процессное представление** (рисунок 18, `diagram_prompts/18_4plus1_process_view.md`): синхронные HTTP-запросы идут через API Gateway к целевому сервису. Ресурсоёмкая realtime-обработка вынесена в асинхронный контур: `session` публикует `SessionEventDTO` в Redis, `realtime_worker` забирает событие и рассылает его подключённым клиентам по WebSocket.

**Представление уровня разработки** (рисунок 19, `diagram_prompts/19_4plus1_development_view.md`): каждый сервис содержит слои `api`, `service`, `models`, `Dockerfile`, `ci.yml`. Общие контракты — в `shared-contracts`. Монолит `ASTROLL_OLD` остаётся отдельным репозиторием/папкой для сравнения.

**Физическое представление** (рисунок 20, `diagram_prompts/20_4plus1_physical_view.md`): набор Docker-контейнеров из `docker-compose.yml`; внешние запросы — через API Gateway; данные — PostgreSQL, Redis, MinIO.

**Сценарий (+1):** ключевой сценарий — игрок перемещает токен персонажа на доске во время сессии:

1. Клиент отправляет команду `MoveTokenCommand` через API Gateway в сервис `session`;
2. `session` проверяет права через `SessionPolicy`, выполняет команду, формирует `SessionEventDTO`;
3. `session` публикует событие в Redis (broker);
4. `realtime_worker` получает событие и рассылает его всем участникам комнаты;
5. Клиенты обновляют отображение доски без перезагрузки страницы.

4 Выводы

По результатам выполнения практической работы была выбрана для проектирования система **ASTROLL** — веб-платформа для проведения настольных ролевых игр онлайн. Для визуализации взаимодействия пользователей с системой построены UML-диаграммы вариантов использования для ролей «Мастер» и «Игрок» (рисунки 1–2, `diagram_prompts/01_use_case_player.md`, `diagram_prompts/02_use_case_master.md`).

Составлены и проанализированы требования на всех уровнях: бизнес-требования (клуб «Энросс‑Клуб»), требования пользователей (User Story для игрока, мастера и администратора), функциональные и нефункциональные требования.

В реальной жизни источниками бизнес-требований могли бы выступать стратегические цели клуба по стандартизации ведения игр, договорённости с игроками об онлайн-формате, экономические показатели (сокращение времени подготовки к сессии). Требования пользователей собирались бы через интервью с мастерами и игроками и наблюдение за сессиями. Функциональные требования выводились бы декомпозицией бизнес- и пользовательских требований.

Выбран архитектурный паттерн, оформлен ADR: начальная реализация — **модульный монолит** (`ASTROLL_OLD`), целевая после роста нагрузки — **сервисная архитектура** (`ASTROLL`).

Система разобрана по методологии DDD: выделены поддомены (смысловое ядро «Игровая сессия (VTT)», вспомогательные и общие поддомены), составлена карта контекстов (рисунок 3) и диаграммы контекста C4 для ядра и поддомена «Персонажи и листы» (рисунки 4–5).

В коде реализованы паттерны проектирования каждого вида: **Фабричный метод** (`board/elements.py`), **Адаптер** (`board/adapters.py`), **Команда** (`game_session/commands.py`).

Реализованы принципы SOLID (OCP, LSP, DIP) и принципы организации компонентов (REP, CCP, ADP, SDP) — граф зависимостей в `components.py`.

Реализованы три способа обеспечения безопасности: RBAC и доменные политики доступа, хеширование паролей и JWT, контейнерная изоляция с API Gateway.

Выполнена реструктуризация архитектуры: монолит `ASTROLL_OLD` преобразован в сервисную платформу `ASTROLL` с общими контрактами, асинхронным `realtime_worker` и инфраструктурой в `docker-compose.yml`. Выбраны атрибуты качества масштабируемости и отказоустойчивости; обновлённая архитектура описана в нотации 4+1 (рисунки 17–20).